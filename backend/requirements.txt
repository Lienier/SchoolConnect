# SchoolConnect backend dependencies
# Pinned to compatible minimum versions; adjust as needed for your environment.

Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-Migrate>=4.0
Flask-JWT-Extended>=4.6
Flask-Mail>=0.9
Flask-Cors>=4.0
Flask-Limiter>=3.5
Flask-Caching>=2.1
Flask-SocketIO>=5.3
simple-websocket>=1.0

SQLAlchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.1

pydantic>=2.6
email-validator>=2.1
python-dotenv>=1.0
bcrypt>=4.1

# QR codes
qrcode[pil]>=7.4

# Google OAuth verification
google-auth>=2.28

# Production file storage
cloudinary>=1.39

# WSGI server (production)
gunicorn>=21.2; platform_system != "Windows"
waitress>=3.0; platform_system == "Windows"

# Test tooling
pytest>=8.0
