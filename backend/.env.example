# SchoolConnect backend environment configuration
# Copy to `.env` and fill in real values. Never commit `.env`.

# Core
FLASK_ENV=development
APP_NAME=SchoolConnect
SECRET_KEY=replace-with-a-long-random-string
API_PREFIX=/api

# Database
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/schoolconnect
TEST_DATABASE_URL=sqlite:///:memory:

# JWT
JWT_SECRET_KEY=replace-with-a-long-random-string
JWT_ACCESS_TOKEN_MINUTES=15
JWT_REFRESH_TOKEN_DAYS=7
# CORS (comma-separated)
CORS_ORIGINS=http://localhost:5173,https://school-connect-ashen.vercel.app

# Rate limiting
RATELIMIT_STORAGE_URI=memory://
RATELIMIT_DEFAULT=200 per hour

# File storage
STORAGE_BACKEND=local
UPLOAD_FOLDER=uploads
MAX_CONTENT_LENGTH_MB=10
ALLOWED_UPLOAD_EXTENSIONS=png,jpg,jpeg,gif,pdf,docx,xlsx

# Cloudinary (production)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# Optional first administrator. Existing accounts are never modified unless the
# temporary recovery flag is explicitly enabled.
SEED_ADMIN_EMAIL=
SEED_ADMIN_PASSWORD=
SEED_ADMIN_RESET_EXISTING=false

# Logging
LOG_LEVEL=INFO
