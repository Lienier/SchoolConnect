# SchoolConnect frontend environment
# Copy to `.env` (or `.env.local`) and adjust. Only VITE_-prefixed vars are exposed.

# Base URL for API requests. Leave as /api to use the Vite dev proxy.
VITE_API_BASE_URL=/api

# Backend origin the dev server proxies /api to.
VITE_API_TARGET=http://localhost:5000
